'use strict';


const { expect } = require('code');
const Lab = require('lab');
const lab = exports.lab = Lab.script();
const { describe, it } = lab;

describe('Checking that the testing', () => {

	it('should work', () => {
		expect(1).to.equal(1);
	});
});
